import type { $ZodStringFormats } from "../core/checks.js";
import type * as errors from "../core/errors.js";
import * as util from "../core/util.js";

const error: () => errors.$ZodErrorMap = () => {
  const Sizable: Record<string, { unit: string; verb: string }> = {
    string: { unit: "karakter", verb: "legyen" },
    file: { unit: "byte", verb: "legyen" },
    array: { unit: "elem", verb: "legyen" },
    set: { unit: "elem", verb: "legyen" },
  };

  function getSizing(origin: string): { unit: string; verb: string } | null {
    return Sizable[origin] ?? null;
  }

  const parsedType = (data: any): string => {
    const t = typeof data;

    switch (t) {
      case "number": {
        return Number.isNaN(data) ? "NaN" : "szÃ¡m";
      }
      case "object": {
        if (Array.isArray(data)) {
          return "tÃ¶mb";
        }
        if (data === null) {
          return "null";
        }

        if (Object.getPrototypeOf(data) !== Object.prototype && data.constructor) {
          return data.constructor.name;
        }
      }
    }
    return t;
  };

  const Nouns: {
    [k in $ZodStringFormats | (string & {})]?: string;
  } = {
    regex: "bemenet",
    email: "email cÃ­m",
    url: "URL",
    emoji: "emoji",
    uuid: "UUID",
    uuidv4: "UUIDv4",
    uuidv6: "UUIDv6",
    nanoid: "nanoid",
    guid: "GUID",
    cuid: "cuid",
    cuid2: "cuid2",
    ulid: "ULID",
    xid: "XID",
    ksuid: "KSUID",
    datetime: "ISO idÅ‘bÃ©lyeg",
    date: "ISO dÃ¡tum",
    time: "ISO idÅ‘",
    duration: "ISO idÅ‘intervallum",
    ipv4: "IPv4 cÃ­m",
    ipv6: "IPv6 cÃ­m",
    cidrv4: "IPv4 tartomÃ¡ny",
    cidrv6: "IPv6 tartomÃ¡ny",
    base64: "base64-kÃ³dolt string",
    base64url: "base64url-kÃ³dolt string",
    json_string: "JSON string",
    e164: "E.164 szÃ¡m",
    jwt: "JWT",
    template_literal: "bemenet",
  };

  return (issue) => {
    switch (issue.code) {
      case "invalid_type":
        return `Ã‰rvÃ©nytelen bemenet: a vÃ¡rt Ã©rtÃ©k ${issue.expected}, a kapott Ã©rtÃ©k ${parsedType(issue.input)}`;
      // return `Invalid input: expected ${issue.expected}, received ${util.getParsedType(issue.input)}`;
      case "invalid_value":
        if (issue.values.length === 1)
          return `Ã‰rvÃ©nytelen bemenet: a vÃ¡rt Ã©rtÃ©k ${util.stringifyPrimitive(issue.values[0])}`;
        return `Ã‰rvÃ©nytelen opciÃ³: valamelyik Ã©rtÃ©k vÃ¡rt ${util.joinValues(issue.values, "|")}`;
      case "too_big": {
        const adj = issue.inclusive ? "<=" : "<";
        const sizing = getSizing(issue.origin);
        if (sizing)
          return `TÃºl nagy: ${issue.origin ?? "Ã©rtÃ©k"} mÃ©rete tÃºl nagy ${adj}${issue.maximum.toString()} ${sizing.unit ?? "elem"}`;
        return `TÃºl nagy: a bemeneti Ã©rtÃ©k ${issue.origin ?? "Ã©rtÃ©k"} tÃºl nagy: ${adj}${issue.maximum.toString()}`;
      }
      case "too_small": {
        const adj = issue.inclusive ? ">=" : ">";
        const sizing = getSizing(issue.origin);
        if (sizing) {
          return `TÃºl kicsi: a bemeneti Ã©rtÃ©k ${issue.origin} mÃ©rete tÃºl kicsi ${adj}${issue.minimum.toString()} ${sizing.unit}`;
        }

        return `TÃºl kicsi: a bemeneti Ã©rtÃ©k ${issue.origin} tÃºl kicsi ${adj}${issue.minimum.toString()}`;
      }
      case "invalid_format": {
        const _issue = issue as errors.$ZodStringFormatIssues;
        if (_issue.format === "starts_with") return `Ã‰rvÃ©nytelen string: "${_issue.prefix}" Ã©rtÃ©kkel kell kezdÅ‘dnie`;
        if (_issue.format === "ends_with") return `Ã‰rvÃ©nytelen string: "${_issue.suffix}" Ã©rtÃ©kkel kell vÃ©gzÅ‘dnie`;
        if (_issue.format === "includes") return `Ã‰rvÃ©nytelen string: "${_issue.includes}" Ã©rtÃ©ket kell tartalmaznia`;
        if (_issue.format === "regex") return `Ã‰rvÃ©nytelen string: ${_issue.pattern} mintÃ¡nak kell megfelelnie`;
        return `Ã‰rvÃ©nytelen ${Nouns[_issue.format] ?? issue.format}`;
      }
      case "not_multiple_of":
        return `Ã‰rvÃ©nytelen szÃ¡m: ${issue.divisor} tÃ¶bbszÃ¶rÃ¶sÃ©nek kell lennie`;
      case "unrecognized_keys":
        return `Ismeretlen kulcs${issue.keys.length > 1 ? "s" : ""}: ${util.joinValues(issue.keys, ", ")}`;
      case "invalid_key":
        return `Ã‰rvÃ©nytelen kulcs ${issue.origin}`;
      case "invalid_union":
        return "Ã‰rvÃ©nytelen bemenet";
      case "invalid_element":
        return `Ã‰rvÃ©nytelen Ã©rtÃ©k: ${issue.origin}`;
      default:
        return `Ã‰rvÃ©nytelen bemenet`;
    }
  };
};

export default function (): { localeError: errors.$ZodErrorMap } {
  return {
    localeError: error(),
  };
}





