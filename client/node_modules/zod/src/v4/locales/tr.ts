import type { $ZodStringFormats } from "../core/checks.js";
import type * as errors from "../core/errors.js";
import * as util from "../core/util.js";

export const parsedType = (data: any): string => {
  const t = typeof data;

  switch (t) {
    case "number": {
      return Number.isNaN(data) ? "NaN" : "number";
    }
    case "object": {
      if (Array.isArray(data)) {
        return "array";
      }
      if (data === null) {
        return "null";
      }

      if (Object.getPrototypeOf(data) !== Object.prototype && data.constructor) {
        return data.constructor.name;
      }
    }
  }
  return t;
};

const error: () => errors.$ZodErrorMap = () => {
  const Sizable: Record<string, { unit: string; verb: string }> = {
    string: { unit: "karakter", verb: "olmalÄ±" },
    file: { unit: "bayt", verb: "olmalÄ±" },
    array: { unit: "Ã¶ÄŸe", verb: "olmalÄ±" },
    set: { unit: "Ã¶ÄŸe", verb: "olmalÄ±" },
  };

  function getSizing(origin: string): { unit: string; verb: string } | null {
    return Sizable[origin] ?? null;
  }

  const Nouns: {
    [k in $ZodStringFormats | (string & {})]?: string;
  } = {
    regex: "girdi",
    email: "e-posta adresi",
    url: "URL",
    emoji: "emoji",
    uuid: "UUID",
    uuidv4: "UUIDv4",
    uuidv6: "UUIDv6",
    nanoid: "nanoid",
    guid: "GUID",
    cuid: "cuid",
    cuid2: "cuid2",
    ulid: "ULID",
    xid: "XID",
    ksuid: "KSUID",
    datetime: "ISO tarih ve saat",
    date: "ISO tarih",
    time: "ISO saat",
    duration: "ISO sÃ¼re",
    ipv4: "IPv4 adresi",
    ipv6: "IPv6 adresi",
    cidrv4: "IPv4 aralÄ±ÄŸÄ±",
    cidrv6: "IPv6 aralÄ±ÄŸÄ±",
    base64: "base64 ile ÅŸifrelenmiÅŸ metin",
    base64url: "base64url ile ÅŸifrelenmiÅŸ metin",
    json_string: "JSON dizesi",
    e164: "E.164 sayÄ±sÄ±",
    jwt: "JWT",
    template_literal: "Åžablon dizesi",
  };

  return (issue) => {
    switch (issue.code) {
      case "invalid_type":
        return `GeÃ§ersiz deÄŸer: beklenen ${issue.expected}, alÄ±nan ${parsedType(issue.input)}`;
      case "invalid_value":
        if (issue.values.length === 1) return `GeÃ§ersiz deÄŸer: beklenen ${util.stringifyPrimitive(issue.values[0])}`;
        return `GeÃ§ersiz seÃ§enek: aÅŸaÄŸÄ±dakilerden biri olmalÄ±: ${util.joinValues(issue.values, "|")}`;
      case "too_big": {
        const adj = issue.inclusive ? "<=" : "<";
        const sizing = getSizing(issue.origin);
        if (sizing)
          return `Ã‡ok bÃ¼yÃ¼k: beklenen ${issue.origin ?? "deÄŸer"} ${adj}${issue.maximum.toString()} ${sizing.unit ?? "Ã¶ÄŸe"}`;
        return `Ã‡ok bÃ¼yÃ¼k: beklenen ${issue.origin ?? "deÄŸer"} ${adj}${issue.maximum.toString()}`;
      }
      case "too_small": {
        const adj = issue.inclusive ? ">=" : ">";
        const sizing = getSizing(issue.origin);
        if (sizing) return `Ã‡ok kÃ¼Ã§Ã¼k: beklenen ${issue.origin} ${adj}${issue.minimum.toString()} ${sizing.unit}`;
        return `Ã‡ok kÃ¼Ã§Ã¼k: beklenen ${issue.origin} ${adj}${issue.minimum.toString()}`;
      }
      case "invalid_format": {
        const _issue = issue as errors.$ZodStringFormatIssues;
        if (_issue.format === "starts_with") return `GeÃ§ersiz metin: "${_issue.prefix}" ile baÅŸlamalÄ±`;
        if (_issue.format === "ends_with") return `GeÃ§ersiz metin: "${_issue.suffix}" ile bitmeli`;
        if (_issue.format === "includes") return `GeÃ§ersiz metin: "${_issue.includes}" iÃ§ermeli`;
        if (_issue.format === "regex") return `GeÃ§ersiz metin: ${_issue.pattern} desenine uymalÄ±`;
        return `GeÃ§ersiz ${Nouns[_issue.format] ?? issue.format}`;
      }
      case "not_multiple_of":
        return `GeÃ§ersiz sayÄ±: ${issue.divisor} ile tam bÃ¶lÃ¼nebilmeli`;
      case "unrecognized_keys":
        return `TanÄ±nmayan anahtar${issue.keys.length > 1 ? "lar" : ""}: ${util.joinValues(issue.keys, ", ")}`;
      case "invalid_key":
        return `${issue.origin} iÃ§inde geÃ§ersiz anahtar`;
      case "invalid_union":
        return "GeÃ§ersiz deÄŸer";
      case "invalid_element":
        return `${issue.origin} iÃ§inde geÃ§ersiz deÄŸer`;
      default:
        return `GeÃ§ersiz deÄŸer`;
    }
  };
};

export default function (): { localeError: errors.$ZodErrorMap } {
  return {
    localeError: error(),
  };
}





